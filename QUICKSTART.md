# Game Manager 快速启动指南

## 环境要求

- Node.js 16+ 
- Windows 系统（用于转区功能）

## 安装步骤

### 1. 安装 Node.js
如果尚未安装，请访问 https://nodejs.org/ 下载并安装 LTS 版本。

### 2. 安装依赖
在项目目录下打开命令行，运行：

```bash
npm install
```

如果安装过程中出现网络问题，可以尝试使用淘宝镜像：

```bash
npm config set registry https://registry.npmmirror.com
npm install
```

### 3. 启动应用

#### 方法1：使用启动脚本
双击运行 `start.bat`

#### 方法2：命令行启动
```bash
npm start
```

## 首次使用

### 1. 配置工具
1. 点击左侧"设置"
2. 配置以下工具路径（可选）：
   - **翻译工具**：用于 Galgame 的翻译软件（如 VNR、Textractor 等）
   - **MTool 翻译**：MTool 的启动文件（exe 或 bat），配置后主界面会显示"启动 MTool"按钮
   - **Locale Emulator**：转区工具路径
3. 选择 Galgame 的默认启动方式（转区/不转区）
4. 点击"保存设置"

### 2. 添加游戏

#### 方法1：手动添加
1. 点击"添加游戏"按钮
2. 填写游戏名称
3. 选择游戏类型（Galgame/RPG/Unity）
4. 选择程序文件（exe 或 bat）
5. 选择存档文件夹（可选）
6. 点击"添加"

#### 方法2：拖拽添加
直接将 exe 或 bat 文件拖拽到主界面的拖放区域即可。

### 3. 编辑攻略
1. 点击游戏卡片的"攻略"按钮
2. 使用工具栏添加节点：
   - **一入一出**：一个输入口，一个输出口
   - **多入一出**：多个输入口，一个输出口
   - **一入多出**：一个输入口，多个输出口
3. **节点操作**：
   - **拖动**：拖动节点标题栏移动位置（连接线会实时跟随）
   - **重命名节点**：点击节点标题直接编辑
   - **重命名子节点**：点击入口/出口标签直接编辑
   - **添加端口**：点击 + 按钮
   - **删除端口**：鼠标悬停显示 - 按钮，点击删除
   - **删除节点**：点击右上角 ×
4. **创建连接**：
   - 从红色输出口拖动到青色输入口
5. **删除连接**：
   - 双击连接线
6. 关闭弹窗自动保存攻略

## 常见问题

### Q: 安装依赖时出错？
A: 尝试以下方法：
1. 清除 npm 缓存：`npm cache clean --force`
2. 使用淘宝镜像：`npm config set registry https://registry.npmmirror.com`
3. 删除 node_modules 后重新安装：`rmdir /s node_modules && npm install`

### Q: 游戏无法启动？
A: 请检查：
1. 程序文件是否存在
2. 对于 Galgame，检查转区工具配置是否正确
3. 尝试以管理员身份运行 Game Manager

### Q: 攻略数据丢失？
A: 攻略数据保存在用户目录下的 `.game-manager/guides.json` 文件中，请勿手动删除此文件夹。

## 数据备份

游戏数据和设置存储在：
```
C:\Users\[用户名]\.game-manager\
```

建议定期备份此文件夹。
